# MinIO Configuration
# Copy to .env and set strong credentials before starting

# MinIO root credentials — must be set; do NOT use defaults in production
MINIO_ROOT_USER=<choose-a-username>
MINIO_ROOT_PASSWORD=<choose-a-strong-password>
